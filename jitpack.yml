jvm:
  - 17

before_install:
  - ./gradlew clean

install:
  - ./gradlew -S -i :burial_plugin:publishToMavenLocal
  - ./gradlew -S -i :burialTimer:publishReleasePublicationToMavenLocal

build:
  - ./gradlew build
